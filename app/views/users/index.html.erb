<% @users.each do |user| %>
<%= render user %>
<div id="<%= dom_id user %>" class="show-user">
  <p>
    <%= link_to t('views.common.show', name: User.model_name.human.downcase), user %>
  </p>
</div>
<% end %>

<%= paginate @users %>

<%= link_to t('views.common.back', name: Book.model_name.human.downcase), books_path %>
