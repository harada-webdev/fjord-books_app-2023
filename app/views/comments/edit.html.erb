<h1>Edit this comment</h1>

<%= form_with model: [@commentable, @comment] do |form| %>
  <% if @comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this report from being saved:</h2>

      <ul>
        <% @comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.label :body, "body" %>
  <%= form.text_area :body, required: true %>
  <%= form.submit "Update this comment" %>
<% end %>

<%= link_to "Back to comments", @commentable %>
