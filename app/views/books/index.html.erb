<p style="color: green"><%= notice %></p>

<h1><%= t('views.common.title.list' ,name: Book.model_name.human) %></h1>

<div id="books" class="index-items">
  <% @books.each do |book| %>
  <div class="index-item">
    <%= render book %>
    <p>
      <%= link_to t('views.common.links.show' ,name: Book.model_name.human.downcase), book %>
    </p>
  </div>
  <% end %>
</div>

<%= link_to t('views.common.links.create' ,name: Book.model_name.human.downcase), new_book_path %>
